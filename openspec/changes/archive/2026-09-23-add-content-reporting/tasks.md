## 1. Database

- [x] 1.1 Create migration `create_reports_table` adding the `reports` table (`id`, `user_id`, `type`, `content_area`, `king_work_id`, `short_story_id`, `adaptation_id`, `related_work_id`, `description`, `created_at`) with the `reports_item_reference_matches_type` check constraint, per `design.md` - Decisions. Verify: `supabase migration up` applies cleanly against local Supabase with no errors.
- [x] 1.2 Add RLS policies on `reports` - insert-by-owner and select-by-owner, no update/delete - per `design.md` - Decisions ("RLS: insert-only-then-owner-read"). Verify: inserting a report as one local test user, then querying `reports` as a second local test user via the Supabase client, returns zero rows for the second user.
- [x] 1.3 Manually insert one row of each `type` (`issue` with each of the four item FKs in turn, and `missing_content`) via local SQL to confirm the check constraint accepts every valid shape and rejects an `issue` row with zero or two item FKs set, and a `missing_content` row with any item FK set.

## 2. Composable

- [x] 2.1 Create `composables/useReports.ts` exposing `reportIssue({ contentArea, itemId, description })` and `reportMissingContent({ contentArea, description })`, both funneling into one internal insert against `reports` per `design.md` - Decisions ("One composable, `useReports()`"). Verify: `pnpm run typecheck` passes with no errors in the new file.
- [x] 2.2 Have both functions reject client-side (without querying Supabase) when `description` is empty or over 500 characters, matching `specs/content-reporting/spec.md` - "Report requires a non-empty description within a length limit". Verify: calling each function with an empty string and with a 501-character string throws/returns a validation error without a network call.

## 3. Report UI components

- [x] 3.1 Create `components/report/Modal.vue` (`<ReportModal>`) - a `UModal` with a description `UTextarea` (500-char limit, live character count) and footer Cancel/Submit buttons per `nuxt-conventions` - "Modal action buttons: footer placement, order, color, variant". Accepts props for `mode` (`issue` | `missing-content`), `contentArea`, and an optional `itemId`. Verify: `pnpm run typecheck` and `pnpm run lint` pass. (Built as a single body-slot submit button, matching `SuggestionCreateModal`'s established "no separate cancel button" pattern for a creation form - `nuxt-conventions` explicitly carves this out as a distinct, equally valid pattern from the footer Cancel/Submit pair.)
- [x] 3.2 Create `components/report/Button.vue` (`<ReportButton>`) - renders the trigger control (label varies by `mode`, per props), and on activation either opens `<ReportModal>` (signed-in) or routes to sign-in (signed-out), per `specs/content-reporting/spec.md` - "Report controls require sign-in". Verify: `pnpm run typecheck` and `pnpm run lint` pass.
- [x] 3.3 Wire `<ReportModal>`'s submit action to `useReports()` and show a success toast/state on completion, matching `specs/content-reporting/spec.md` - "Submitting with a valid description" scenario. Verify: manually submit the form while signed in against local Supabase and confirm a row appears in `reports` with the expected `type`/`content_area`/item FK. (Wiring verified by code inspection plus the equivalent direct-SQL insert already exercised in 1.3, which used the same row shapes the composable produces - the actual through-the-browser click-through is covered by 6.2, since per this project's CLAUDE.md the Nuxt dev server isn't started as part of implementation.)

## 4. Detail page integration

- [x] 4.1 Add `<ReportButton mode="issue" content-area="works" :item-id="work.id" />` to `app/pages/works/[slug].vue`. Verify: the control renders on a work's detail page and opens the issue-report modal when signed in. (Placed in the hero, directly below the work's description, per user feedback that the control belonged there rather than at the bottom of the page.)
- [x] 4.2 Add the equivalent `<ReportButton mode="issue" content-area="short_works" :item-id="story.id" />` to `app/pages/short-works/[slug].vue`. Verify: same as 4.1, on a short story's detail page. (Placed in the hero, below the Dark Tower relation note - short stories have no `description` field of their own.)
- [x] 4.3 Add the equivalent `<ReportButton mode="issue" content-area="adaptations" :item-id="adaptation.id" />` to `app/pages/adaptations/[slug].vue`. Verify: same as 4.1, on an adaptation's detail page. (Placed in the hero, below the TMDb overview/description.)
- [x] 4.4 Add the equivalent `<ReportButton mode="issue" content-area="works_by_others" :item-id="relatedWork.id" />` to `app/pages/works-by-others/[slug].vue` (`work.id` in that page's own variable naming). Verify: same as 4.1, on a by-other-hands item's detail page. (Placed in the hero, below the description.)

## 5. Overview page integration

- [x] 5.1 Add `<ReportButton mode="missing-content" content-area="works" />` to `app/pages/works/index.vue`. Verify: the control renders on the works overview page and opens the missing-content modal when signed in. (Added a `#header-actions` slot to the shared `BibliographyBrowsePage.vue` - used by all 4 overview pages - rather than duplicating header markup per page. Per user feedback, iterated on placement/sizing twice: first next to the title, which pushed the title on narrow screens; then moved into its own row alongside the page description, wrapping together with it, sized `sm`, styled as a `link` variant.)
- [x] 5.2 Add the equivalent `<ReportButton mode="missing-content" content-area="short_works" />` to `app/pages/short-works/index.vue`. Verify: same as 5.1, on the short works overview page.
- [x] 5.3 Add the equivalent `<ReportButton mode="missing-content" content-area="adaptations" />` to `app/pages/adaptations/index.vue`. Verify: same as 5.1, on the adaptations overview page.
- [x] 5.4 Add the equivalent `<ReportButton mode="missing-content" content-area="works_by_others" />` to `app/pages/works-by-others/index.vue`. Verify: same as 5.1, on the works-by-others overview page.

## 6. End-to-end verification

- [x] 6.1 Signed out, confirm activating a report control on any one detail page and any one overview page routes to sign-in without opening a form, per `specs/content-reporting/spec.md` - "Signed-out visitor activates a report control". (Verified manually by the user.)
- [x] 6.2 Signed in, submit one `issue` report and one `missing_content` report end to end (through the actual UI against local Supabase) and confirm both rows land in `reports` with the correct `type`, `content_area`, item reference (or none), and `user_id`. (Verified manually by the user.)
- [x] 6.3 Run `pnpm run typecheck` and `pnpm run lint` across the whole change and confirm both pass with no new errors. (Both pass for every file this change touches. `pnpm run typecheck` still reports 2 pre-existing errors in `useBooks.ts` and `profile/[username]/compare.vue` - confirmed via `git stash` to already exist on `develop` before this change, unrelated to `reports`/`useReports`/`ReportButton`/`ReportModal`/the 8 touched pages.)

## 7. Public reports overview and admin status (added after initial implementation, per user request)

- [x] 7.1 Create migration `add_reports_status_and_public_view` adding a `status` column (`new`/`rejected`/`applied`, default `new`) to `reports`, replacing the owner-only select policy with an authenticated-read policy, adding an admin-only status-update policy plus the `revoke`/`grant (status)` column-level narrowing, and creating the `reports_public` view (resolves each report's item title/slug across the four source tables, never selects `user_id`) - per `design.md` - Decisions ("RLS: owner-only insert, authenticated-read, admin-only status update"). Verify: `supabase migration up` applies cleanly.
- [x] 7.2 Verify via direct SQL: a report inserted by one simulated user is readable by a different simulated authenticated user through `reports_public` (with `user_id` absent from its columns); a non-admin's status update is rejected by RLS; an admin's status update succeeds; an admin cannot change `description` in the same update (column-level grant holds even for admins).
- [x] 7.3 Add `isAdmin`, `fetchReports({ page, pageSize, status })`, and `updateReportStatus(id, status)` to `composables/useReports.ts`, reading from `reports_public` and writing `status` only to the base `reports` table. Verify: `pnpm run typecheck` passes.
- [x] 7.4 Create `components/report/List.vue` (`<ReportList>`) - an accordion list (mirroring `SuggestionList.vue`'s structure) showing each report's type, content area, linked item (if any, as a link built from `item_slug`/`content_area`), status badge, and description in the expanded body; renders an admin-only status `USelect` per `specs/content-reporting/spec.md` - "Only admins can change a report's status". Verify: `pnpm run typecheck` and `pnpm run lint` pass.
- [x] 7.5 Add a "Reports" section to `app/pages/suggestion-box.vue` - status filter (defaulting to `new`, per `specs/content-reporting/spec.md` - "Reports section can be filtered by status"), `<ReportList>`, and pagination, following the same load/watch/paginate pattern already used for suggestions on that page. Verify: `pnpm run typecheck` and `pnpm run lint` pass.
- [x] 7.6 Update `specs/content-reporting/spec.md` and `design.md` to replace the "Reports are not publicly visible" requirement/Non-Goal with the actual public-browsable, admin-status-workflow behavior now implemented, so the planning artifacts stay consistent with the code.

## 8. Admin delete (added after initial implementation, per user request)

- [x] 8.1 Create migration `add_reports_delete_policy` adding an admin-only delete RLS policy on `reports`, mirroring `add_suggestions_delete_policy.sql` - per `design.md` - Decisions ("RLS..."). Verify: `supabase migration up` applies cleanly.
- [x] 8.2 Verify via direct SQL: a non-admin's delete is rejected by RLS (row still present afterward); an admin's delete succeeds.
- [x] 8.3 Add `deleteReport(id)` to `composables/useReports.ts`, a plain base-table delete relying on the admin-only RLS policy. Verify: `pnpm run typecheck` passes.
- [x] 8.4 Create `components/report/DeleteModal.vue` (`<ReportDeleteModal>`) - a confirmation modal mirroring `SuggestionDeleteModal.vue` (footer Cancel/Delete, `color="error"` on Delete per `nuxt-conventions`' destructive-modal exception). Verify: `pnpm run typecheck` and `pnpm run lint` pass.
- [x] 8.5 Wire a delete button into `<ReportList>` (admin-only, alongside the status select) and `onReportDeleted` into `suggestion-box.vue` (same last-item-on-a-later-page page-back adjustment as suggestions' `onDeleted`), per `specs/content-reporting/spec.md` - "Only admins can delete a report". Verify: `pnpm run typecheck` and `pnpm run lint` pass.
- [x] 8.6 Update `specs/content-reporting/spec.md` and `design.md` with the delete requirement/policy, so the planning artifacts stay consistent with the code.
