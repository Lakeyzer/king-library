## 1. Database

- [x] 1.1 Write a local migration creating `user_book_reads` (`id`, `user_id`, `king_work_id`, `read_on` date nullable, `read_year` int nullable, `note` text nullable with a max-200-character check constraint, `format` text nullable constrained to `physical`/`audiobook`/`ebook`, `rating` int nullable constrained to 1-5) and verify it applies cleanly with `supabase migration up` against local Supabase.
- [x] 1.2 Add the four owner-or-public-profile RLS policies to `user_book_reads` (select/insert/update/delete), matching the existing `user_books` pattern, and verify with `supabase migration up` plus a manual query as a non-owner confirming rows are hidden unless the owner's profile is public.
- [x] 1.3 Write a backfill migration or script that inserts one `user_book_reads` row for every existing `user_books` row with `read = true`, sourced from its `started_on`/`finished_on`/`read_year`, and verify row counts match (`select count(*) from user_books where read` equals `select count(*) from user_book_reads`) on local Supabase after running it.
- [x] 1.4 Update `.claude/skills/supabase-conventions/SKILL.md` to document `user_book_reads` (schema, RLS, the two-write convention with `user_books`) and remove/update the "known limitation" note it currently carries about reread history.

## 2. Composable changes (`useBooks()`)

- [x] 2.1 Add a `logRead(workId, { readOn?, readYear?, note?, format?, rating? })` function that inserts a `user_book_reads` row and upserts `user_books` (`read = true`, `started_on`/`finished_on`/`read_year` updated same as today), and verify it against local Supabase with a manual call that produces both rows. (Implemented as `insertLoggedRead()`, called by `finishReading`/`markRead`/`readAgain` — see design.md note on why `finishReading`'s `user_books` write couldn't share `markRead`'s upsert directly. Verified by replaying the exact write pattern via SQL against local Supabase.)
- [x] 2.2 Update `finishReading()` to accept the same optional note/format/rating and call the shared logging path from 2.1 instead of only updating `user_books`, and verify a finish produces both a `user_books` update and a new `user_book_reads` row.
- [x] 2.3 Update `markRead()` to accept optional note/format/rating alongside its existing date/year params and call the shared logging path from 2.1, and verify existing callers (direct mark-read, `EditReadDatesModal`) still compile and behave the same when the new fields are omitted. (`nuxt typecheck` passes; `EditReadDatesModal.vue` was rewritten in section 4 to target `updateLoggedRead` instead, per its own task.)
- [x] 2.4 Add a `readAgain(workId, { readOn?, readYear?, note?, format?, rating? })` function usable when `user_books.read` is already true, using the same shared logging path, and verify it against a work already marked read that repeated calls each create a new distinct `user_book_reads` row without touching earlier ones. (Verified via SQL: two simulated calls produced two independent `user_book_reads` rows with distinct note/format/rating, `user_books` reflecting only the latest.)
- [ ] 2.5 **Superseded by 6.2** — product feedback changed unmark from "preserve history" to "confirm, then delete all logged reads and clear dates." See section 6.
- [x] 2.6 Change `fetchReadingTimeline()` to query `user_book_reads` (joined to `king_works` for title/slug/cover) instead of `user_books`, ordered most-recent first by `coalesce(read_on, ...)`/`read_year`, and returning note/format/rating per entry; verify with a manual call against a user with multiple logged reads on the same work that the same work appears more than once. (Verified the equivalent SQL ordering directly against local Supabase — two entries returned, most-recent first.)
- [x] 2.7 Add an `updateLoggedRead(readId, { readOn?, readYear?, note?, format?, rating? })` function for editing one `user_book_reads` row, and have it also sync `user_books.started_on`/`finished_on`/`read_year` when the edited row is that work's most-recent logged read; verify both the single-row edit and the most-recent-sync case manually. (Verified via SQL: editing the older of two reads left `user_books` untouched; editing the most-recent one synced `user_books.finished_on`.)

## 3. UI: read-completing prompts

- [x] 3.1 Extend the mark-as-read/finish prompt component with a note textarea (200-character limit shown and enforced), a format select (physical/audiobook/ebook), and a 1-5 star rating input, all optional, and verify by completing a read with each field populated and confirming the values reach `markRead`/`finishReading`. **Code complete** (new `BookReadDetailsFields.vue`, wired into `MarkReadModal.vue` and `FinishReadingModal.vue`; `nuxt typecheck` passes) — the in-app verification is left unchecked per CLAUDE.md's testing workflow (no dev server started at wrap-up); ready for manual testing.
- [x] 3.2 Add a "Read again" control shown only when a work is marked read (compact and expanded reading-status control presentations), wired to open the same prompt and call `readAgain()`, and verify it appears only in the read state and creates a new logged read on confirm. **Code complete** (`ReadingActions.vue`: compact dropdown item + expanded-mode buttons in both the max-sm and sm+ layouts, opening `MarkReadModal` with `mode="again"`) — in-app verification pending, same as 3.1.
- [x] 3.3 Manually verify the "Marking read with logged details" and "Confirming a finish with logged details" scenarios from `specs/reading-status/spec.md` end to end in the running app. **Pending** — requires the running app; left for manual testing per CLAUDE.md.

## 4. UI: reading timeline

- [x] 4.1 Update `ReadingTimeline.vue` to render one entry per `ReadingTimelineEntry` from the updated `fetchReadingTimeline()`, keyed by the logged-read id rather than the work id, and verify a work with two logged reads renders two timeline entries. **Code complete** (`localItems`/`timelineItems`/`handleSaved` all keyed by `readId`; underlying multi-entry-per-work data flow verified via SQL in 2.6) — visual confirmation in the running app pending.
- [ ] 4.2 **Superseded by 8.6** — product feedback moved note/format/rating display off the horizontal showcase widget entirely; they now render only on the dedicated Reading Timeline page (vertical mode). See section 8 and the revised profile-showcase spec.
- [x] 4.3 Update the timeline's edit-date affordance (`EditReadDatesModal.vue` or its replacement) to call `updateLoggedRead()` for the specific entry being edited, including the note/format/rating fields, and verify editing one entry does not change any other logged read's displayed details. **Code complete** (`EditReadDatesModal.vue` rewritten around `readId`/`workId` and all five fields; the most-recent-sync vs. older-entry behavior it depends on was verified via SQL in 2.7) — in-app verification pending.

## 5. Manual verification

- [x] 5.1 Confirm `work_stats.read_count`, showcase progress bars, and category-completion percentages are unchanged for a user who rereads a work multiple times (still counts once), by comparing values before and after logging a reread in the running app. **Pending** — requires the running app; these views/queries were not touched by this change (still read `user_books.read`), but the end-to-end confirmation is left for manual testing per CLAUDE.md.
- [x] 5.2 Confirm the full reread flow end to end in the running app: mark a work read, finish it, read it again with different note/format/rating, and see two distinct entries on the showcase's reading timeline (dates only) and on the dedicated Reading Timeline page (with their own note/format/rating), while stats still show it as read once. **Pending** — requires the running app; left for manual testing per CLAUDE.md.

## 6. Unmark read: confirm, then delete all logged reads

- [x] 6.1 Add a confirmation modal (`BookUnmarkReadModal.vue` or similar; plain copy, not the Bookshelf-removal ka-tet phrasing — see design.md) that names the work and warns that its logged reads (notes/formats/ratings included) will be permanently deleted, and verify it renders with Cancel/confirm actions. **Code complete** (`BookUnmarkReadModal.vue`) — in-app verification pending, per CLAUDE.md.
- [x] 6.2 Change `unmarkRead(workId)` to delete every `user_book_reads` row for that `(user_id, king_work_id)` and clear `started_on`/`finished_on`/`read_year` in the same operation as flipping `read` to `false`, and verify via SQL against local Supabase that a work with multiple logged reads ends up with zero `user_book_reads` rows and null dates afterward.
- [x] 6.3 Wire `ReadingActions.vue`'s unmark control (compact dropdown primary action and the expanded-mode "Mark as Unread" button) to open the confirmation modal instead of calling `unmarkRead()` directly, calling it only on confirm. **Code complete** — in-app verification pending, per CLAUDE.md.

## 7. Start Reading available from an already-read work

- [x] 7.1 Write a migration altering `clear_read_states_on_progress()` so it only clears `want_to_read`/`currently_reading` when `read` is transitioning to `true` in this write (`old.read is distinct from true`, same pattern as `cascade_short_story_reads_on_collection_read`), rather than whenever `read` merely ends up `true` — see design.md "Starting a new read on an already-read work" for why the original unconditional check fights `startReading()` once a work can be restarted after being read. Verify via SQL against local Supabase that: (a) want-to-read → currently-reading still clears want-to-read, (b) neutral/currently-reading → read (via finish/markRead) still clears want-to-read and currently-reading, and (c) starting a read on an already-read, not-currently-reading work now results in `currently_reading = true` (previously it would have been silently reverted to `false`).
- [x] 7.2 Update `finishReading()` and the shared `markRead`/`readAgain` write to explicitly set `currently_reading: false` in their own `user_books` write, rather than relying solely on the trigger — needed for the case where `read` was already `true` before this session started (the trigger's transition-only condition from 7.1 no longer fires there). Verify via SQL that finishing a second read on a work already read before that session began correctly ends with `currently_reading = false`.
- [x] 7.3 In `ReadingActions.vue`, reprioritize `primaryState` to check `currently_reading` before `read` (so the primary action while mid-reread is "Finish," even on an already-read work), and remove the `read`-state restriction on the start/finish expanded-mode button (`canStartOrFinishReading`) so "Start Reading" is available whenever the work isn't already currently-reading, regardless of read status. **Code complete** — reprioritized `primaryState`; removed `canStartOrFinishReading` entirely (no state disables the control now); the expanded-mode split-button branch is now keyed off `primaryState === 'read'` instead of raw `isRead`, so the currently-reading-and-already-read coexistence case correctly shows "Finish"/"Mark as Read" rather than "Mark as Unread"/"Read Again" (matching the updated spec). `nuxt typecheck` passes. In-app verification pending, per CLAUDE.md.
- [x] 7.4 Add "Start Reading" to the compact dropdown's read-state item list (alongside "Mark as Unread" and "Read Again") so the same action is reachable from both presentations. **Code complete** — in-app verification pending.

## 8. Dedicated Reading Timeline page

- [x] 8.1 Add an `orientation?: 'horizontal' | 'vertical'` prop to `ReadingTimeline.vue` (default `'horizontal'`), rendering the existing drag-scroller/chevron chrome only in horizontal mode and a plain vertical `UTimeline` otherwise, sharing the same item-mapping/edit-modal/detail-rendering logic between both. Verify the default (no prop passed) renders identically to before for the existing showcase usage.
- [x] 8.2 Create `ProfileTimelineTab.vue` (mirroring `ReadListTab.vue`'s structure) that fetches via `fetchReadingTimeline()` (same `useAsyncData` key as the showcase widget) and renders `ProfileReadingTimeline` with `orientation="vertical"`, including an empty state.
- [x] 8.3 Create `app/pages/profile/timeline.vue` (own-shortcut) and `app/pages/profile/[username]/timeline.vue` (by-username), mirroring `read-list.vue`'s two variants exactly for sign-in/privacy/SEO handling, both rendering `ProfileTimelineTab`.
- [x] 8.4 Add an end-justified link/button on the same line as the Showcase's "Reading Journey" heading, pointing at the signed-in viewer-appropriate timeline path (`/profile/timeline` for the owner, `/profile/{username}/timeline` otherwise) computed by `ReaderChecklistTab.vue` and passed down to `ProfileShowcase` as a prop. Verify the link's `href` for both the owner's-own-showcase case and a visited public profile's case.
- [x] 8.5 Manually verify in the running app: the heading link navigates to the timeline page, the page shows a vertical timeline with personal ratings visible, and the private-profile / signed-out-own-page states behave the same as Read List's. **Pending** — requires the running app, per CLAUDE.md.
- [x] 8.6 Remove the `#description` slot (note/format/rating) from `ReadingTimeline.vue`'s horizontal branch only — the showcase widget shows just work and date (see the revised profile-showcase spec) — and give the vertical branch a centered zigzag layout instead of UTimeline's default single-column vertical list. **Revised three times**: (1) the first `:ui` override attempt (absolute-positioned `container`, half-width `wrapper`) didn't actually alternate anything in the running app - replaced with Nuxt UI's own documented alternating-layout recipe (https://ui.nuxt.com/docs/components/timeline#with-alternating-layout). (2) that recipe alone rendered a smaller indicator than the horizontal branch and let each side's text sprawl across the full width with no cap, overlapping the opposite side - fixed by explicitly matching the horizontal branch's `indicator: 'size-12'` (recalculating the recipe's translate constants for 3rem instead of the `size="sm"`-derived 1.75rem) and adding `wrapper: 'sm:max-w-[calc(50%-2.25rem)]'`. (3) node spacing was too tight, and the format/rating row inside `#description` stayed left-aligned on the reversed (left) side even though the note text next to it correctly followed the alternation - fixed with an ungated `wrapper: 'pb-10'` for spacing, and `sm:group-even:justify-end` on the format/rating row specifically (`text-align` only repositions inline content, not a block-level flex row, which is why the note's text was already fine but this row wasn't - see design.md "`text-align` doesn't reposition block-level flex rows"). See design.md "Vertical zigzag layout follows Nuxt UI's documented recipe" for the full derivation. `nuxt typecheck` passes; horizontal widget's note/format/rating removal is unaffected by any of these revisions. Visual confirmation (indicator size, column alignment, spacing, and the format/rating row's alignment) is still pending, per CLAUDE.md.

## 9. Format captured at Start Reading, shown on the Currently Reading card

- [x] 9.1 Write a migration adding a nullable `format` column to `user_books` (same `physical`/`audiobook`/`ebook` check constraint as `user_book_reads.format`), and verify it applies cleanly plus rejects an invalid value, against local Supabase.
- [x] 9.2 Extract the shared `format` label/icon/select-options mappings into `app/utils/readFormat.ts` (`READ_FORMAT_LABEL`, `READ_FORMAT_ICON`, `READ_FORMAT_OPTIONS`), and update `ReadingTimeline.vue` and `ReadDetailsFields.vue` to use it instead of their own local copies. Verify `nuxt typecheck` passes with no behavior change to either.
- [x] 9.3 Add an optional format field to `StartReadingModal.vue`, and update `startReading(workId, startedOn, format?)` to write it to `user_books.format` (explicitly nulled when omitted, same reasoning as `markRead`'s dates). Verify via SQL against local Supabase that a supplied format is stored and an invalid one is rejected by the constraint.
- [x] 9.4 Update `fetchCurrentlyReading()` and `CurrentlyReadingWork` to include `format`, and fix every other constructor of that type (`useFollowing.ts`'s following-feed fetch) to satisfy the now-required field. Verify `nuxt typecheck` passes.
- [x] 9.5 In `CurrentlyReading.vue`, show a format icon (`READ_FORMAT_ICON`) immediately before each work's title when a format is present, and make the title visible at every viewport width (previously `lg:hidden`) rather than only below `lg`. **Code complete** — `nuxt typecheck` passes; visual confirmation pending, per CLAUDE.md.

## 10. Add to Timeline: search and log a read from the Reading Timeline page

- [x] 10.1 Create `BookSearchWorkModal.vue`: fetches active King works once (lazily, on first open) via `useKingWorks().fetchKingWorks()`, and shows a `USelectMenu` searchable by title, emitting the selected `KingWork` and closing itself. Verify `nuxt typecheck` passes.
- [x] 10.2 Add a `logged` emit to `MarkReadModal.vue`, fired after a successful `markRead`/`readAgain` call, so a caller whose data isn't part of the shared `userBooksByWorkId` state (like the Reading Timeline page) can react. Verify existing callers (`ReadingActions.vue`) still compile unchanged, since they don't need to listen for it.
- [x] 10.3 Add an "Add to Timeline" button to `ProfileTimelineTab.vue` (owner-only), wired to open `BookSearchWorkModal`; on a work being selected, open `BookMarkReadModal` (default `mode="mark"`) for it; on that modal's `logged` event, call the page's `useAsyncData` `refresh()` so the newly logged read appears without a full reload. **Code complete** - `nuxt typecheck` passes; in-app verification (search filtering, selection opening the mark-read prompt, the new entry appearing after confirming) is pending, per CLAUDE.md.

## 11. Fix expanded-mode control overcrowding on work detail pages

- [x] 11.1 Reduce `ReadingActions.vue`'s expanded mode to 3 slots (plus Shelf) by repurposing the leftmost slot per state instead of adding a control per new action: readlist toggle (neutral/want-to-read, unchanged), hidden entirely via `v-if` while currently-reading (was previously rendered disabled, contributing to the crowding even when useless), and Mark as Unread once read (moved off the read-related slot, which now holds only Read Again in that state). No action was removed - see design.md "Expanded-mode controls: slots are repurposed per state, not accumulated" for why this needed no reading-status spec change. **Code complete** - `nuxt typecheck` passes; visual confirmation that this actually resolves the overflow on work detail pages at every screen size is pending, per CLAUDE.md.
- [x] 11.2 In `DetailHero.vue` (shared by work/adaptation/short-work detail pages), move the `#stats` slot below the `#actions` box and outside its `bg-elevated` background - implemented as two copies of the stats slot content (one inside the actions box, one as a sibling below it), since a single slot's DOM position can't itself respond to a breakpoint. **Revised**: initially scoped to `md` only; changed to span from the smallest width through `md` (matching what mobile already did) and centered the same way mobile's stats already were, reverting to the original side-by-side-inside-the-box layout only at `lg+`. Final breakpoints: outside-box/centered copy visible by default through `md` (`lg:hidden`), inside-box copy `hidden` until `lg:flex`, and the actions box itself only gains its `bg-elevated`/row layout at `lg:`. **Code complete** - `nuxt typecheck` passes; visual confirmation (centering, and that lg+ still looks like the original side-by-side box) is pending, per CLAUDE.md.
- [x] 11.3 In `DetailHero.vue`, add `items-center` to the actions box's own classes so `#actions`' button row (which sizes to content, not full width, below `lg`) is horizontally centered rather than left-aligned. Also fixed a real ordering bug found while doing this: the standalone stats sibling (11.2) had no `order` class, so at mobile it tied the cover image's default `order: 0` instead of the actions box's `max-sm:order-2` - being later in the DOM than the cover but tied with it, it rendered between the cover and the actions box (stats above the buttons) instead of after both. Fixed by giving the stats sibling the same `max-sm:order-2` and letting DOM position (it comes after the actions box in the template) break the tie in the same direction sm+ already resolves it via the order-0 tie there. **Code complete** - `nuxt typecheck` passes; visual confirmation (centering, and that mobile ordering is now cover → buttons → stats → description) is pending, per CLAUDE.md.

## 12. Icons on the format dropdown

- [x] 12.1 Derive `READ_FORMAT_OPTIONS` from `READ_FORMAT_LABEL`/`READ_FORMAT_ICON` instead of a separately-hardcoded label list, adding an `icon` field to each option (`USelect`'s item type supports `icon` natively). Both consumers (`ReadDetailsFields.vue`'s format field, used by Mark as Read/Finish/Read Again, and `StartReadingModal.vue`'s format field) pick this up automatically since they already reference the shared list. **Code complete** - `nuxt typecheck` passes; visual confirmation pending, per CLAUDE.md.

## 13. Vertical timeline: format icons and owner-only notes

- [x] 13.1 In `ReadingTimeline.vue`'s vertical `#description` slot, show `READ_FORMAT_ICON[item.format]` alongside the format label. Gate the note paragraph (and the outer detail wrapper's visibility check) behind `isOwner`, so a note - personal, per product feedback - never renders for a visitor viewing someone else's public Reading Timeline, only for the page's own owner. Updated the reading-timeline spec (which previously, and incorrectly, said entries "match the same per-entry detail shown by the reading timeline widget on the owner's showcase" - that widget shows no details at all as of section 8.6, so this also fixes a stale cross-reference). **Code complete** - `nuxt typecheck` passes; visual confirmation (icon rendering, and that notes are actually absent for a non-owner visitor) is pending, per CLAUDE.md.

## 14. Currently Reading card redesign: horizontal, matches recommendations, subtly distinguished

- [x] 14.1 Redesign `CurrentlyReading.vue` as a WorkTile-style horizontal row (matching the recommendation cards' size/layout exactly - `ImageThumbnail` `size="xs"`, title + subtitle stacked next to it) instead of the previous responsive vertical/centered layout at `lg:`. Subtitle is the format's label and icon (when captured). The per-item Finish action is now icon-only (a bare checkmark, `aria-label` for accessibility) instead of a labeled button. Moved the "Currently Reading" heading from `Showcase.vue` into the component itself, matching how each recommendation card owns its own heading inside its own `bg-elevated` box.
- [x] 14.2 Since the "Currently Reading" heading moved inside the component, `Showcase.vue`'s sidebar (which also holds the recommendation cards) lost the heading it was implicitly using as its own section title - added a new "Spotlight" heading (with a sparkles icon, matching the other section headings' size/style) at the sidebar level.
- [x] 14.3 Added a subtle visual distinction (a `ring-1 ring-primary/30` outline plus a primary-colored heading icon) so Currently Reading doesn't blend into the recommendation cards it now otherwise matches exactly in layout and size, per product feedback ("should stand out in a way, subtly").
- [x] 14.4 Fixed the Finish modal not prefilling the format that was captured at Start Reading time: added an `initialFormat` prop to `FinishReadingModal.vue` (seeded into its `format` ref both on mount and on reopen), wired from `userBook.format` in `ReadingActions.vue` and from the `CurrentlyReadingWork.format` already available in `CurrentlyReading.vue`. Updated the reading-status spec's finish requirement to describe the prefill (and that it's still adjustable before confirming).
- [x] 14.5 Updated the profile-showcase spec: format now described as a label+icon shown beneath the title (not "immediately before" it, which was the old vertical-layout wording), dropped the now-meaningless "title remains visible at every viewport width" scenario (layout no longer varies by breakpoint at all), and added a scenario for the new visual-distinction requirement.

All of 14.1-14.4 are **code complete** - `nuxt typecheck` passes; visual/behavioral confirmation (card sizing matches recommendations, the ring is visible but subtle, the Spotlight heading reads well, and the format actually shows up prefilled in the Finish modal) is pending, per CLAUDE.md.

## 15. End-to-end confirmation

- [x] 15.1 User tested the full feature set end to end in the running app and confirmed it looks good (2026-09-17) - resolves every "visual/in-app confirmation pending" note left throughout this file under CLAUDE.md's testing workflow (testing is manual, run on demand by the user, not something started automatically at wrap-up). The two remaining unchecked items (2.5, 4.2) are intentional redirects to superseding tasks, not outstanding work.
