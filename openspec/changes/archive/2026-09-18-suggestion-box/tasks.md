## 1. Database

- [x] 1.1 Create a migration adding the `suggestions` table (`id`, `user_id` FK → `auth.users.id`, `title` not-null, `body` not-null, `is_anonymous` boolean default `false`, `status` text default `'new'`, `created_at` timestamptz default `now()`) with non-empty `check` constraints on `title`/`body` and a `check` constraint restricting `status` to `new`/`rejected`/`confirmed`/`applied`, and verify `supabase migration up` applies cleanly against local Supabase
- [x] 1.2 Add the `suggestions readable by authenticated users` (select, `to authenticated`) and `suggestions insertable by owner` (insert, `to authenticated`, `user_id = auth.uid()`) RLS policies in the same migration, with RLS enabled on the table, and verify a signed-out (anon-key, no session) query against `suggestions` returns no rows
- [x] 1.3 Add the `suggestions status updatable by admin` UPDATE policy (`to authenticated`, `using`/`with check` both `(auth.jwt() -> 'app_metadata' ->> 'role') = 'admin'`) plus `revoke update on suggestions from authenticated; grant update (status) on suggestions to authenticated;` in the same migration, and verify an authenticated non-admin's `update suggestions set status = ...` is rejected while an authenticated admin's succeeds
- [x] 1.4 Add the `suggestions_with_author` view (joins `profiles` for `username`, nulls it when `is_anonymous`, selects `status`, never selects `user_id`) in the same migration, and verify querying it as a signed-in user returns `username: null` for an anonymous row and the real username otherwise, with `status` present on every row
- [x] 1.5 Verify an authenticated insert with a mismatched `user_id` (not the caller's own) is rejected by RLS
- [x] 1.6 Manually grant the admin role claim to a local test user (`update auth.users set raw_app_meta_data = raw_app_meta_data || '{"role":"admin"}' on id = '<test-user-id>'` against local Supabase) and verify their session JWT carries `app_metadata.role = 'admin'` after re-authenticating
- [x] 1.7 Add a follow-up migration with `check` constraints capping `title` at 100 characters and `body` at 1000 characters, and verify an insert exceeding either limit is rejected by Postgres while one at exactly the limit succeeds
- [x] 1.8 Add a follow-up migration creating `suggestion_votes` (`id`, `user_id` FK, `suggestion_id` FK, `is_upvote` boolean, `created_at`, unique `(user_id, suggestion_id)`) with RLS enabled and the four policies (select `to authenticated using (true)`, insert/update/delete owner-only), and verify a second insert for the same `(user_id, suggestion_id)` pair is rejected by the unique constraint while an update or delete of the caller's own row succeeds
- [x] 1.9 Verify a non-owner cannot update or delete another user's vote row (RLS denial), and that a signed-out query against `suggestion_votes` returns no rows
- [x] 1.10 In the same migration, add the `suggestion_vote_counts` view (`security_invoker = true`, `count(*) filter (where ...)` for `upvote_count`/`downvote_count`/`score`) and `create or replace view suggestions_with_author` adding `upvote_count`, `downvote_count`, `score`, and `my_vote` (self-vote left join on `auth.uid()`), and verify querying it as two different signed-in users after one of them votes shows the same `score`/counts to both but a different `my_vote` value for each
- [x] 1.11 Add a follow-up migration with a `suggestions deletable by admin` DELETE policy (`to authenticated using (auth.jwt() -> 'app_metadata' ->> 'role' = 'admin')`), and verify a non-admin's `delete from suggestions` matches zero rows while an admin's succeeds and removes the row
- [x] 1.12 Verify deleting a suggestion also removes its `suggestion_votes` rows via the existing `on delete cascade`
- [x] 1.13 Add a follow-up migration replacing the `suggestion_votes` insert/update/delete policies with versions that also require `exists (select 1 from suggestions s where s.id = suggestion_id and s.status = 'new')`, and verify: casting a first vote on a "new" suggestion still succeeds; casting, changing, or undoing a vote on a "rejected"/"confirmed"/"applied" suggestion all match zero rows (RLS denial)
- [x] 1.14 Verify a suggestion's vote counts remain queryable (via `suggestions_with_author`) and unchanged after its status changes away from "new" - only mutation is blocked, not visibility

## 2. Composable

- [x] 2.1 Create `useSuggestions()` with `fetchSuggestions({ page, pageSize, status })` (`status` optionally `'all'` or one of the four statuses, applying `.eq('status', status)` unless `'all'`) reading from `suggestions_with_author` ordered by `created_at desc` via `.range()`, and verify it returns paginated results with `username` and `status` already resolved, filtered correctly when `status` is given
- [x] 2.4 Extend `fetchSuggestions` with a `sort` param (`'newest'` default or `'popular'`), ordering by `created_at desc` or by `score desc, created_at desc` respectively, and verify each sort produces the expected order against seeded rows with differing scores
- [x] 2.5 Add `castVote(suggestionId, isUpvote, currentVote)` to `useSuggestions()` - upserts `{ user_id, suggestion_id, is_upvote }` on the `(user_id, suggestion_id)` conflict key when `currentVote !== isUpvote`, or deletes the caller's own vote row when `currentVote === isUpvote` (the undo case) - and verify: a first vote inserts, a same-direction second call deletes (score returns to its prior value), and an opposite-direction call updates (score moves by 2, not 1)
- [x] 2.6 Add `deleteSuggestion(id)` to `useSuggestions()` (delete against the base `suggestions` table), and verify calling it as a non-admin session leaves the row in place (RLS denial, zero rows affected) while an admin session removes it and it no longer appears in a subsequent `fetchSuggestions()` call
- [x] 2.2 Add `createSuggestion({ title, body, isAnonymous })` inserting into the base `suggestions` table with `user_id` set to the current session's user, and verify a created suggestion appears in a subsequent `fetchSuggestions()` call with `status: 'new'` and the correct `username`/`null` per `isAnonymous`
- [x] 2.3 Add `isAdmin` (computed from the current session's `app_metadata.role === 'admin'`) and `updateSuggestionStatus(id, status)` (update against the base `suggestions` table) to `useSuggestions()`, and verify calling `updateSuggestionStatus` as a non-admin session throws/rejects (RLS denial) while an admin session succeeds and the change is reflected in a subsequent `fetchSuggestions()` call

## 3. Suggestion-creation modal

- [x] 3.1 Build the "New Suggestion" modal component (Nuxt UI `UModal`) with a title input, a body textarea, and an anonymous toggle, and verify submitting with either field empty shows a validation message and does not call `createSuggestion`
- [x] 3.3 Add a 100-character `maxlength` (with a live character count) to the title input and a 1000-character `maxlength` (with a live character count) to the body textarea, plus a validation message when either is exceeded, and verify the form blocks submission over either limit and cannot exceed it in the DB-level check added in 1.7
- [x] 3.2 Wire successful submission to `useSuggestions().createSuggestion()`, close the modal, and refresh/prepend the list, and verify a new suggestion is visible on the page immediately after submitting without a manual reload

## 4. Suggestion Box page

- [x] 4.1 Add the `/suggestion-box` route/page using the default layout, gated to signed-in users (redirect a signed-out visitor to sign-in, matching the pattern used by the Read List page), and verify navigating to it while signed out redirects to sign-in
- [x] 4.2 Render the paginated suggestion list as a `UAccordion` (Nuxt UI), one item per suggestion — header shows title, author display, and a status badge; body text revealed on expand — with pagination controls on the page, and verify navigating between pages loads different suggestions and each item's body is hidden until expanded
- [x] 4.3 Add the "New Suggestion" control that opens the modal from section 3, visible only when signed in, and verify it is absent for a signed-out visitor
- [x] 4.4 Add an inline status control (e.g. `USelect`) to each accordion item, rendered only when `useSuggestions().isAdmin` is true, wired to `updateSuggestionStatus`, and verify it is absent for a non-admin signed-in user and that changing it updates the badge without a manual reload — placed in the expanded body rather than the collapsed header, since `UAccordion`'s header is itself a single button and nesting another interactive control inside it is invalid HTML; the read-only status badge still stays visible collapsed
- [x] 4.5 Add a status filter control above the list (`USelect` with the four statuses plus an "All" option), defaulting to "New", wired to `fetchSuggestions`'s `status` param and resetting the page to 1 on change, and verify a fresh page load shows only new suggestions and switching the filter updates the list accordingly
- [x] 4.6 Add a short message on the page asking visitors to look through existing suggestions before submitting a new one, and verify it is visible on the page
- [x] 4.7 Restructure `SuggestionList.vue` to render one `UCollapsible` per suggestion instead of a single `UAccordion` (see design.md "List item structure") — trigger content (title, author, status badge, chevron) inside the collapsible's default slot; body text inside its content slot — and verify collapsed/expanded behavior is unchanged from before the restructuring. The admin status control moves out of the content slot to sit as an always-visible sibling (per design.md, no longer requires expanding)
- [x] 4.8 Add thumbs-up/thumbs-down vote buttons and a vote-count/score display as true siblings of each row's `UCollapsible` (not inside its trigger), highlighting the pressed direction from `suggestion.myVote`, wired to `useSuggestions().castVote`, and verify voting, changing direction, and undoing (clicking the same direction again) each update the displayed score and highlight without a manual reload
- [x] 4.9 Add a sort control (`USelect`: "Newest" default, "Most Popular") above the list, wired to `fetchSuggestions`'s `sort` param and resetting the page to 1 on change, and verify switching to "Most Popular" reorders the list by score
- [x] 4.10 Add an admin-only delete control (trash icon button) alongside the existing status control, opening a confirmation step before calling `useSuggestions().deleteSuggestion`, and verify it is absent for a non-admin signed-in user, canceling the confirmation leaves the suggestion in place, and confirming removes it from the list without a manual reload
- [x] 4.11 Disable both vote buttons (not hidden - counts stay visible and meaningful) when a suggestion's `status !== 'new'`, and verify voting is only possible on "new" suggestions while vote counts remain visible on every status

## 5. Footer link

- [x] 5.1 Add a link with an icon to `/suggestion-box` in the default layout's footer, visible to every visitor, and verify it renders on a page using the default layout and navigates to the Suggestion Box page

## 6. Homepage card

- [x] 6.1 Build a `HomepageLinkCard` component (icon, title, description, the whole card a link — see design.md "Homepage card: a new small component, not the existing catalog-link shape") distinct from `HomepageCatalogLinkCard`, and verify it renders and navigates to its `to` target
- [x] 6.2 Add it to the homepage's narrower (sidebar) column directly after the stats card, with an icon, the title "Suggestion Box", a description inviting visitors to vote on suggestions, linking to `/suggestion-box`, visible to every visitor, and verify it appears in that position for both signed-in and signed-out visitors

## 7. Manual verification

- [x] 7.1 Walk through: sign out → footer link visible, page redirects to sign-in; sign in → create a named suggestion and an anonymous suggestion → both appear correctly attributed in the paginated list with status "new"
- [x] 7.2 Walk through as an admin: change a suggestion's status through each of the four values and confirm the badge updates each time; sign in as a non-admin and confirm the status badge is visible but no control to change it is present
- [x] 7.3 Walk through voting: upvote a suggestion, confirm the score updates; downvote it instead, confirm the score moves by 2; click the same direction again to undo, confirm the score returns to its original value; switch sort to "Most Popular" and confirm ordering follows score
- [x] 7.4 Walk through deletion as an admin: start deleting a suggestion, cancel, confirm it's still there; delete another suggestion and confirm it's gone from the list; sign in as a non-admin and confirm no delete control is present
- [x] 7.5 As an admin, change a "new" suggestion (with an existing vote) to "confirmed" and confirm its vote buttons become disabled while the score stays visible and unchanged; confirm you can no longer vote, change, or undo a vote on it
- [x] 7.6 View the homepage signed out and signed in, and confirm the Suggestion Box card appears in the sidebar in both cases and navigates to the Suggestion Box page
