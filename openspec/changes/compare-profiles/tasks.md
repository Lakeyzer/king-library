## 1. Diff data layer

- [x] 1.1 Add `fetchReadDiff(userIdA, userIdB)` to `composables/useBooks.ts`, returning `{ onlyA: WorkHighlight[], onlyB: WorkHighlight[] }` computed from two `user_books` (`read = true`) queries plus one `king_works` lookup; verify with a quick manual call (or unit test if the project adds one) against two seeded users confirming the sets are correctly one-sided.
- [x] 1.2 Add `fetchWatchedDiff(userIdA, userIdB)` to `composables/useAdaptations.ts`, mirroring 1.1 against `user_adaptations` (`watched = true`) and `adaptations`; verify the same way.
- [x] 1.3 Add `fetchOwnedDiff(userIdA, userIdB)` to `composables/useBooks.ts`, mirroring 1.1 exactly but filtered on `owned = true` instead of `read = true`; verify the same way with two seeded accounts having non-overlapping and fully-overlapping owned sets.

## 2. Compare route

- [x] 2.1 Create `app/pages/profile/[username]/compare.vue`: resolve the target profile via `useProfile().fetchProfileByUsername`, 404 (`createError({ statusCode: 404 })`) when it resolves to no profile — mirror `pages/profile/[username]/index.vue`; verify by visiting a compare URL for a nonexistent username and confirming a 404.
- [x] 2.2 Redirect a signed-out visitor to sign in when they load the compare route, consistent with the own-Read-List/own-showcase sign-in gate; verify by visiting the route signed out.
- [x] 2.3 Show a "you can't compare a profile with itself" message (no fetch of comparison data) when the resolved target profile's id matches the signed-in user's own id; verify by visiting your own username's compare route.
- [x] 2.4 Show the existing "this profile is private" state (reuse the same empty-state component/copy as `RouteChrome`) when the target profile is private and the signed-in user is not its owner, without fetching comparison data; verify by visiting another user's compare route while their profile is private.

## 3. Comparison layout components

- [x] 3.1 Build a `ProfileCompareRow` component (or equivalent name) that renders two sides in a `flex flex-col sm:flex-row` layout (stacked under `sm`, side-by-side at `sm` and up); verify visually at a small and a large viewport width that a single instance stacks/rows correctly.
- [x] 3.2 Build a compact header for the compare page showing both profiles' avatar + username (signed-in visitor's side and the target's side), reusing `UAvatar`/`NumberMotif` patterns from `Header.vue`; verify it renders both identities correctly.
- [x] 3.3 Build `ProfileCompareProgressCard` (stacked horizontal `UProgress` bars, one per user: a label line with just the username, then a row with the bold `count / total` on the left and the percentage on the right, bottom-`items-end`-aligned, then the bar itself, `primary` color for the signed-in visitor and `warning` for the other user, a `title` attribute per bar for the hover count/total) and render the five progress categories (Overall Bibliography, Bachman Books, Dark Tower, Collection, Adaptations Watched) as five of these cards in a `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4`, left-aligned category titles, using the appropriate user's `ProfileBookStats`/`ViewingProgress` values (call `fetchProfileBookStats`/`fetchViewingProgress` once per user id, in parallel via `Promise.all`); verify each category's card shows both users' bars/counts/percentages/usernames correctly for two seeded accounts with different progress, that the count/total and percentage sit on one line bottom-aligned, that hovering a bar shows its count/total, and that the grid shows 3/2/1 columns at large/medium/small widths.
- [x] 3.3b Add a difference badge (`UBadge`, `variant="subtle"`) to each `ProfileCompareProgressCard`'s title row, justified to the end of that line: `success` color + up-arrow icon + `+N` when the signed-in visitor's count is higher, `error` color + down-arrow icon + the negative difference when lower, no badge on a tie; verify all three cases against two seeded accounts with categories where the visitor leads, trails, and ties.
- [x] 3.4 Render the read-books diff as one `ProfileCompareRow` (left: books only the signed-in visitor has read, right: books only the target has read), using `fetchReadDiff`, each side showing an empty state when its list is empty, each item linking to its work detail page (`/works/[slug]` or equivalent per `read-list`'s pattern); verify with two seeded accounts having non-overlapping and fully-overlapping read sets.
- [x] 3.5 Render the watched-adaptations diff the same way as 3.4 using `fetchWatchedDiff`, items linking to their adaptation detail page; verify the same way with watched adaptations.
- [x] 3.4b Build `ProfileCompareDiffCard` (title, muted count description, and a slot in a fixed-height `h-70 overflow-y-auto` box) and wrap each side of both `ProfileCompareRow` instances (3.4, 3.5) in one. Verify the list scrolls once it exceeds ~5 items and the count in the description matches the list length for both non-overlapping and fully-overlapping seeded sets.
- [x] 3.4c Render the owned-books diff as a third `ProfileCompareRow`, same shape as 3.4 but using `fetchOwnedDiff`; verify with two seeded accounts having non-overlapping and fully-overlapping owned sets.
- [x] 3.4d Give each `ProfileCompareRow` its own `title`/`icon` ("Books Read" / "Books Owned" / "Adaptations Watched", each with a matching icon) instead of leaving those props unused; shrink each `ProfileCompareDiffCard`'s title to `"By {username}"` (`NumberMotif`-wrapped, since it's now dynamic) and its description to drop the now-redundant leading username (`"{N} read books {other username} hasn't."` / `"{N} owned books {other username} doesn't."` / `"{N} watched adaptations {other username} hasn't."`). Verify each row shows its own category header once, each card shows "By {username}", and the description's verb still makes each row's category clear at a glance.
- [x] 3.6 Assemble `compare.vue`'s body from the header (3.2), a `Total Activity` `<h2>` + its bar (3.6b), a `Reading Progress` `<h2>` + the progress-category grid (3.3), and the three `ProfileCompareRow` diff-list blocks (3.4, 3.4c, 3.5, 3.4d) in a single `flex flex-col gap-*` stack — every named block, including each `ProfileCompareRow`'s own title, renders the same `<h2>` markup (`text-base font-semibold` + `size-5` icon) sitting outside that block's card/grid/card-pair; verify the progress grid shows multiple cards per row while each diff-list block still renders alone, one at a time, and that all five headings on the page (`Total Activity`, `Reading Progress`, and the three row titles) read as the same size.

- [x] 3.6b Add a `UProgressGroup` "Total Activity" bar (headed by its own `<h2>`, see 3.6) above the category grid: two items (`own`/`target`), each item's `value` the sum of that user's books-read count, books-owned count, and adaptations-watched count, with `max` set to the sum of both items' values so the bar is always completely full and split proportionally between the two users; a label row (color dot + username, `NumberMotif`-wrapped) and a count row sit underneath the bar, own on the left and target on the right in both. Verify the bar is always full for two seeded accounts with different activity levels, and that the split visually matches the ratio of their combined counts.
- [x] 3.6c Add a `USelectMenu` (`multiple`) on the same line as the `Total Activity` heading, justified to the end (`flex justify-between`), with items Books Read / Books Collected / Adaptations Watched (all selected by default) bound to a `selectedActivityCategories` ref; make `ownTotalActivity`/`targetTotalActivity` only sum a source's count when its category is selected. Verify deselecting a category narrows the bar (still always full, just over fewer sources) for two seeded accounts, and that reselecting it restores the original combined total.

## 4. Compare entry point

- [x] 4.1 Add a "Compare" link/button to `Header.vue`'s non-owner branch, shown only when `isPublic` is true, linking to `/profile/${username}/compare`, placed alongside the existing follow/unfollow button; verify it appears for a signed-in visitor on another user's public profile, and is absent when signed out, when viewing your own profile, or when the profile is private.

## 5. Manual verification

- [ ] 5.1 Walk through the full flow as a signed-in user: open another public profile, click Compare, confirm all five progress categories and all three diff-list pairs (read, owned, watched) render correctly, then resize the browser across the `sm` breakpoint and confirm every block switches between row and column layout.
